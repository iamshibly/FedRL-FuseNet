# Baseline Backbone Comparison

This file is reserved for the baseline backbone comparison experiment.

The full notebook was output-heavy, so the GitHub-viewable Markdown version will be regenerated from the local notebook.

Source notebook name:

`6-arcf-net-baseline-backbone-compare.ipynb`
